import {createTheme} from "@mui/material";


const theme = createTheme({
    typography: {
        fontFamily: [
            "Work Sans",
            "Roboto",
            "Lato"
        ].join(',')
    },
});

export default theme;
